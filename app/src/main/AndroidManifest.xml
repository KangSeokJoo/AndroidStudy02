<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jinasoft.study02_ksj">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:resizeableActivity="false"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">
        <activity android:name=".Callback.CallbackMain"></activity>
        <activity android:name=".Fragment.FragMain" />
        <activity android:name=".AdapterView.AdapterView" />
        <activity android:name=".DisplayRotation.DisplayRoatation" />
        <activity android:name=".Direct.AppListActivity" />
        <activity android:name=".Direct.DirectGo" />
        <activity android:name=".EmailSave.EmailSave" />
        <activity android:name=".SelectStudyList">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>